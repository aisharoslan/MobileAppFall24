// Always capitalize name of file for react components (to distinguish from script)
// will make a components folder if >1 component

// creating a component named App
function App() {
  // do a bunch of JS here
  // whatever we return here is the component
  return (
    // this is JSX
    // go thru React and babel - then html
    <div>
      <h2>Hello React!</h2>
      </div>
  );
}

export default App;
